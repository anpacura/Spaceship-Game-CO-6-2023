from pygame.font import Font
from game.utils.constants import FONT_STYLE, SCREEN_WIDTH, SCREEN_HEIGHT

def get_message(message, size, color, width - SCREEN_WIDTH//2, height):
    font = Font(FONT_STYLE, size)